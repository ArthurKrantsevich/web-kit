---
"@web-kit/json-core": minor
"@web-kit/json-formatter": minor
---

New package @web-kit/json-core: lossless AST (parseJson), printJson with key sorting, paths, stats and exact number comparison. json-formatter now builds on it. Error columns are counted in characters, the code frame never splits emoji, and "Insert missing comma" is only offered before real literals.
