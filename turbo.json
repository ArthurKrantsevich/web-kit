{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["out/**", "dist/**", ".next/**", "!.next/cache/**"]
    },
    "check": {
      "dependsOn": ["build"]
    }
  }
}
